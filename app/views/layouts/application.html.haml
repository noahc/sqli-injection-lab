!!!
%html{lang: "en"}
  %head
    %meta{charset: "utf-8"}/
    %meta{content: "IE=edge", "http-equiv" => "X-UA-Compatible"}/
    %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}/
    %meta{content: "", name: "description"}/
    %meta{content: "", name: "author"}/
    %title STANLEY - Free Bootstrap Theme
    %link{href: "/assets/bootstrap.css", rel: "stylesheet"}/
    %link{href: "/assets/main.css", rel: "stylesheet"}/
    / HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries
    /[if lt IE 9]
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
  %body
    / Static navbar
    .navbar.navbar-inverse.navbar-static-top
      .container
        .navbar-header
          %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", type: "button"}
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand{href: "index.html"} BreakerBank
        .navbar-collapse.collapse
          %ul.nav.navbar-nav.navbar-right
            - if current_user
              %li
                %a{href: "work.html"} My Page
              %li
                =link_to "Accounts", accounts_path
              %li
                =link_to "Transactions", transactions_path
              %li
                %a
                  = "Welcome, #{current_user.email}"
              %li
                %a
                  = button_to "Sign Out", sign_out_path, :method => :delete
            - else
              %li
                %a{href: "/"} Please Login

    - flash.each do |key, msg|
      %div{ class: "alert alert-#{key}"}
        %p 
          = msg
    #ww
      .container
        .row
          .col-lg-2
            - if current_user
              %h3 Quick Account Transfer
              = form_tag Transaction.new do 
                .field
                  = label_tag :account
                  = select_tag "account", options_for_select(current_user.accounts.pluck(:account_number, :id))
                .field
                  = label_tag :To
                  = select_tag "to", options_for_select(User.pluck(:name))
                .field
                  = label_tag :Amount
                  = text_field_tag :amount
                = submit_tag :submit
          .col-lg-8.col-lg-offset-2.centered
            = yield
    #footer
      .container
        .row
          .col-lg-4
            %h4 My Bunker
            %p
              1337 Hacker Road,
              %br/
              832.232.8234,
              %br/
              BreakerVille, USA.
          .col-lg-4
          .col-lg-4
            %h4 About Breaker Bank
            %p Proudly Serving the Citizens of Breakerville since 1982. Bringing you the best service and securest banking man can provide.
